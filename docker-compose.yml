version: '3.8'

services:
  scentgraph:
    build:
      context: .
      dockerfile: Dockerfile
    image: scentgraph:latest
    container_name: scentgraph_app
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - HOST=0.0.0.0
    restart: unless-stopped

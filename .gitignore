# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
env/
venv/
ENV/
.env

# Unit test / coverage
.pytest_cache/
.coverage
htmlcov/

# Node / Frontend
node_modules/
frontend/node_modules/
dist/
# Note: keep frontend/dist so Docker or static serving can use it without rebuilding if desired, or let build create it.

# Temporary raw downloads (can be re-downloaded via python data/build_dataset.py)
data/raw/

# IDE / System
.vscode/
.idea/
.agents/
.gemini/

services:
  - type: web
    name: scentgraph
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 10000
      - key: HOST
        value: 0.0.0.0

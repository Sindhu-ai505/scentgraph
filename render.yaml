services:
  - type: web
    name: scentgraph
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 8080
      - key: HOST
        value: 0.0.0.0
